name: C/C++ CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: windows-Arduino_Sotware

    steps:
    - uses: actions/checkout@v4
    - name: Smart_Home Automation
      run: ./LightModule
